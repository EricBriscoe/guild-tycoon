DISCORD_TOKEN=
DISCORD_CLIENT_ID=
# Optional: use for faster iteration with a single guild (recommended while developing)
DISCORD_GUILD_ID=

# Background passive production ticker (optional)
# Set to 'false' to disable. Defaults to true.
GT_BACKGROUND_TICK_ENABLED=true
# Interval in milliseconds between background ticks (min 1000). Defaults to 15000.
GT_BACKGROUND_TICK_MS=15000
# Log a summary line per tick (guilds/users/total gained). Defaults to false.
GT_BACKGROUND_TICK_LOG=false
